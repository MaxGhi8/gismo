\documentclass{article}
\usepackage[english]{babel}
\usepackage[letterpaper,top=2cm,bottom=2cm,left=3cm,right=3cm,marginparwidth=1.75cm]{geometry}
\usepackage{lmodern}
\usepackage[T1]{fontenc}

% Useful packages
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{graphicx}
\usepackage[colorlinks=true, allcolors=blue]{hyperref}
\usepackage{booktabs}
\usepackage{subcaption}
\usepackage{microtype}
\usepackage{placeins} % \FloatBarrier
% algorithms
\usepackage{algorithm}
\usepackage{algpseudocode}
\algdef{SE}[PARFOR]{ParFor}{EndParFor}[1]{\textbf{parfor} $#1$ \textbf{do}}{\textbf{end parfor}}
% bibliography
\usepackage{authblk}
\usepackage{csquotes}
%\usepackage[style=numeric, backend=bibtex]{biblatex}
%\addbibresource{references.bib}
% tikz
\usepackage{tikz}
\usetikzlibrary{positioning, arrows.meta, fit, shapes.geometric, calc, shadows}
% notes
\usepackage{todonotes}
% To show the labels of sections, equations, figures, references, etc.
\usepackage{showkeys}
\renewcommand*\showkeyslabelformat[1]{%
  \fbox{\parbox[t]{\marginparwidth}{\raggedright\normalfont\tiny\ttfamily#1}}}
% To do notes
\setlength{\marginparwidth}{2cm}
\newcommand{\mg}[1]{\todo[color=green!20]{\textbf{MG:} #1}}
\newcommand{\mginline}[1]{\todo[color=green!20, inline]{\textbf{MG:} #1}}
\newcommand{\al}[1]{\todo[color=orange!20]{\textbf{AL:} #1}}
\newcommand{\alinline}[1]{\todo[color=orange!20, inline]{\textbf{AL:} #1}}

% numeri di riga
\usepackage[switch]{lineno}

\title{A Geometry-Aware Isogeometric Neural Preconditioner for Partial Differential Equations}
\date{}
\author[1]{Massimiliano Ghiotto}
\author[2]{Alessandro Lucantonio}
\affil[1]{Department of Mathematics, University of Pavia}
\affil[2]{Department of Mechanical and Production Engineering, Aarhus University}



% User defined commands
\newcommand{\vect}[1]{\boldsymbol{#1}}
\newcommand{\R}{\mathbb{R}}
\newcommand{\N}{\mathbb{N}}
\newcommand{\skel}{\Gamma} % skeleton (interface) index set
\DeclareMathOperator{\spann}{span}
\newcommand{\npatch}{K} % number of patches

% -----------------------------------------------------------------------
% Notation macros
% -----------------------------------------------------------------------
% Domains
\newcommand{\refdom}{\widehat{\Omega}}            % reference/parameter domain
\newcommand{\patchdom}[1]{\Omega_{#1}}            % k-th patch physical domain

% Basis functions
\newcommand{\bsuni}[2]{\widehat{b}_{#1,#2}}      % univariate B-spline
\newcommand{\Bref}[2]{\widehat{B}_{#1,#2}}       % reference tensor-product B-spline
\newcommand{\Bphy}[2]{B_{#1,#2}}                  % physical (push-forward) basis function
\newcommand{\Bloc}[2]{B^{(#1)}_{#2}}              % local patch basis (patch index, DOF index)
\newcommand{\Rnurbs}[2]{\widehat{R}_{#1,#2}}     % NURBS (rational) basis

% Function spaces
\newcommand{\Shat}[1]{\widehat{\mathcal{V}}_{#1}} % reference spline space (uni- or multivariate)
\newcommand{\Vglo}{\mathcal{V}_h}                  % global conforming IGA space
\newcommand{\Vloc}[1]{\mathcal{V}_{#1}}            % local patch IGA space
\newcommand{\Vprod}{\mathcal{V}_h^{\Pi}}           % product (broken) space

% Geometry maps
\newcommand{\gmap}{\mathcal{F}}                   % geometry map (single-patch or global context)
\newcommand{\gmapk}[1]{\mathcal{F}_{#1}}         % patch-k geometry map

% Global stiffness and load
\newcommand{\Aglo}{A}                             % global stiffness matrix
\newcommand{\fglo}{\vect{f}}                      % global load vector
\newcommand{\uglo}{\vect{u}}                      % global solution vector

% Local stiffness and load
\newcommand{\Aloc}[1]{A_{#1}}                    % local stiffness matrix
\newcommand{\Aaug}[1]{\widetilde{A}_{#1}}        % augmented (IETI) stiffness
\newcommand{\floc}[1]{\vect{f}_{#1}}             % local load vector
\newcommand{\faug}[1]{\widetilde{\vect{f}}_{#1}} % augmented load vector

% Jump (continuity) matrices -- J to avoid clash with basis functions B
\newcommand{\Jloc}[1]{J_{#1}}                    % local jump matrix
\newcommand{\Jfull}{J}                            % global jump operator
\newcommand{\Jskel}[1]{\widehat{J}_{#1}}         % jump restricted to skeleton DOFs
\newcommand{\Jaug}[1]{\widetilde{J}_{#1}}        % augmented jump (IETI)

% Schur complements
\newcommand{\Sloc}[1]{S_{#1}}                    % local Dirichlet Schur complement
\newcommand{\Snn}{\mathcal{S}_{\theta}}          % neural Schur operator

% Primal constraints and basis
\newcommand{\Cloc}[1]{C_{#1}}                    % primal constraint matrix
\newcommand{\Psik}[1]{\Psi_{#1}}                 % primal energy-minimising basis
\newcommand{\nprim}{n_\Pi}                        % number of primal DOFs

% Interface (dual) system
\newcommand{\Fschur}{F}                           % interface Schur operator
\newcommand{\gschur}{\vect{g}}                    % interface right-hand side

% Preconditioners
\newcommand{\Psd}{M_{sD}^{-1}}                   % scaled Dirichlet preconditioner
\newcommand{\Pneural}{M_{\theta}^{-1}}            % neural preconditioner

% Neural architecture
\newcommand{\dv}{d_v}                             % latent dimension of Transformer

% Multiplicity scaling
\newcommand{\Dmult}[1]{D_{#1}}                   % multiplicity-weighted diagonal

% Local and augmented solutions
\newcommand{\uloc}[1]{\vect{u}_{#1}}             % local patch solution
\newcommand{\uaug}[1]{\widetilde{\vect{u}}_{#1}} % augmented local solution (IETI)
\newcommand{\lagmult}{\vect{\lambda}}             % Lagrange multipliers

% -----------------------------------------------------------------------

% Theorem environments
\usepackage{amsthm}
\theoremstyle{plain}
\newtheorem{ass}{Assumption}
\newtheorem{lemma}{Lemma}
\newtheorem{prop}{Proposition}
\newtheorem{theorem}{Theorem}
\newtheorem{df}{Definition}
\newtheorem{rmk}{Remark}
\newtheorem{cor}{Corollary}
\numberwithin{equation}{section}


\begin{document}
\maketitle
% \tableofcontents
\linenumbers

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% Abstract
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\begin{abstract}
\end{abstract}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% Section
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Preliminaries} \label{sec:preliminaries}

In this section we fix the notation and recall the principal concepts needed in the
remainder of the paper. We first define B-splines and the associated
isogeometric spaces (Section~\ref{subsec:bsplines}), state the model problem and
its multipatch discretization (Section~\ref{subsec:model}), and summarize the
dual-primal Isogeometric Tearing and Interconnecting (IETI-DP) method
(Section~\ref{subsec:ieti}) \cite{kleiss2012ieti}.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\subsection{B-splines and isogeometric spaces} \label{subsec:bsplines}

Given integers $m, p > 0$, a knot vector is a non-decreasing sequence
$\Xi := \{0 = \xi_1 \le \dots \le \xi_{m+p+1} = 1\}$, which we take \emph{open},
i.e.\ $\xi_1 = \dots = \xi_{p+1} = 0$ and $\xi_{m+1} = \dots = \xi_{m+p+1} = 1$.
The Cox--de Boor recursion~\cite{de1978practical} defines the univariate B-spline
basis functions $\bsuni{i}{p} : [0,1] \to \R$, $i = 1, \dots, m$:
\begin{equation*}
    \bsuni{i}{0}(\eta) =
    \begin{cases}
        1 & \text{if } \xi_i \le \eta < \xi_{i+1}, \\
        0 & \text{otherwise},
    \end{cases}
    \qquad
    \bsuni{i}{p}(\eta) =
    \frac{\eta - \xi_i}{\xi_{i+p} - \xi_i}\, \bsuni{i}{p-1}(\eta)
    + \frac{\xi_{i+p+1} - \eta}{\xi_{i+p+1} - \xi_{i+1}}\, \bsuni{i+1}{p-1}(\eta),
\end{equation*}
with the convention $0/0 = 0$; an interior knot of multiplicity $r$ yields
$C^{p-r}$ continuity~\cite{cottrell2009isogeometric}. The constructed basis functions span the univariate
spline space $\Shat{p} := \spann\{\bsuni{i}{p}\}_{i=1}^{m}$.
Multivariate B-splines are constructed with tensor products; on $\refdom := (0,1)^d$,
with a degree $p_l$ and an open knot vector per direction $l = 1, \dots, d$, we
set $\Bref{\vect{i}}{\vect{p}}(\vect{\eta}) := \prod_{l=1}^{d}
\bsuni{i_l}{p_l}(\eta_l)$ for $\vect{\eta} \in \refdom$. Defining a
colexicographical reordering into a single index $i = 1, \dots, n$, with
$n := \prod_{l=1}^{d} m_l$, gives the reference function space $\Shat{\vect{p}} :=
\spann\{\Bref{i}{\vect{p}}\}_{i=1}^{n}$.

The physical domain $\Omega \subset \R^{D}$ ($D \ge d$) is the image
$\Omega = \gmap(\refdom)$ of a \emph{geometry map}
$\gmap = \sum_{i=1}^{n} \vect{P}_i\, \Bref{i}{\vect{p}} \in
[\Shat{\vect{p}}]^{D}$ with control points $\vect{P}_i \in \R^{D}$.
We assume that the geometry map is invertible with full-rank Jacobian $J_{\gmap} = D\gmap$.
For $D = d$ the domain is planar or volumetric; for $D > d$ it is a manifold (e.g.\ a
surface in $\R^3$), on which physical gradients use the Moore--Penrose
pseudo-inverse $J_{\gmap}^{\dagger} = J_{\gmap}^{\top}
(J_{\gmap} J_{\gmap}^{\top})^{-1}$ of the (non-square) Jacobian\mg{check this}.
By the isoparametric paradigm~\cite{hughes2005isogeometric}, the isogeometric
space on $\Omega$ is
\begin{equation}
    \Vglo := \spann\bigl\{ \Bphy{i}{\vect{p}} := \Bref{i}{\vect{p}} \circ
    \gmap^{-1} \ \big|\ i = 1, \dots, n \bigr\}.
    \label{eq:isogeometric-space}
\end{equation}
NURBS are obtained by assigning weights $w_i > 0$ and replacing
$\Bref{i}{\vect{p}}$ with $\Rnurbs{i}{\vect{p}} = w_i
\Bref{i}{\vect{p}} / \sum_{j=1}^n w_j \Bref{j}{\vect{p}}$ (B-splines
being the case $w_i \equiv 1$); all that follows is unchanged.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\subsection{Model problem and multipatch discretization} \label{subsec:model}

As a model problem we take the scalar diffusion equation on an open, bounded,
connected Lipschitz domain $\Omega \subset \R^{D}$ with $\partial\Omega =
\overline{\Gamma_D} \cup \overline{\Gamma_N}$, $\Gamma_D \cap \Gamma_N =
\emptyset$. Given $\alpha \in L^\infty(\Omega)$ with $\alpha \ge \alpha_0 > 0$,
$f \in L^2(\Omega)$ and boundary data $g_D, g_N$, we seek $u$ with
\begin{equation}
    -\nabla \cdot (\alpha \nabla u) = f \ \text{ in } \Omega,
    \qquad u = g_D \ \text{ on } \Gamma_D,
    \qquad \partial_n u = g_N \ \text{ on } \Gamma_N.
    \label{eq:poisson-strong}
\end{equation}
The weak form reads: find $u \in V_{g_D} := \{ v \in H^1(\Omega) : v_{|\Gamma_D}
= g_D \}$ such that
\begin{equation}
    a(u, v) = F(v) \qquad \forall\, v \in V_0 := \{ v \in H^1(\Omega) :
    v_{|\Gamma_D} = 0 \},
    \label{eq:weak-form}
\end{equation}
with $a(u, v) := \int_{\Omega} \alpha\, \nabla u \cdot \nabla v\,\mathrm{d}\Omega$
and $F(v) := \int_{\Omega} f v \,\mathrm{d}\Omega + \int_{\Gamma_N} g_N v
\,\mathrm{d}s$. If $\Gamma_D = \emptyset$ (a closed surface, or pure Neumann
data), $a(\cdot,\cdot)$ is only positive semidefinite, the problem is then solvable only under the compatibility condition
$\int_\Omega f \,\mathrm{d}\Omega + \int_{\partial\Omega} g_N \,\mathrm{d}s = 0$,
and its solution is unique up to a constant, for example by fixing the zero mean condition $\int_\Omega u
\,\mathrm{d}\Omega = 0$.

We discretize on a non-overlapping multipatch decomposition into $\npatch$
patches,
\begin{equation}
    \overline{\Omega} = \bigcup_{k=1}^{\npatch} \overline{\patchdom{k}},
    \qquad \patchdom{k} \cap \patchdom{l} = \emptyset \ \text{ for } k,l = 1, \ldots, \npatch, \text{ and } k \neq l,
    \label{eq:multipatch}
\end{equation}
each the image $\patchdom{k} = \gmapk{k}(\refdom)$, $k = 1, \ldots, \npatch$, of its own geometry
map, carrying a local isogeometric space $\Vloc{k}$ of dimension $n_k$ as
in~\eqref{eq:isogeometric-space}. The patches are assumed \emph{fully
matching}~\cite{kleiss2012ieti}, adjacent patches share a complete edge/face with
coinciding basis traces, so that the conforming global space $\Vglo :=
\{ v \in H^1(\Omega) : v_{|\patchdom{k}} \in \Vloc{k}, \ k=1, \ldots, \npatch \}$ is well defined. Given a
basis $\{\Bphy{i}{\vect{p}}\}_{i=1}^{N}$ of $\Vglo$, after fixing the Dirichlet boundary conditions we define $N := \dim \Vglo$, and the
Galerkin discretization of~\eqref{eq:weak-form} is the following linear system
\begin{equation}
    \Aglo\, \uglo = \fglo, \qquad
    \Aglo_{ij} = a(\Bphy{j}{\vect{p}}, \Bphy{i}{\vect{p}}), \quad
    \fglo_i = F(\Bphy{i}{\vect{p}}), \quad i, j = 1, \dots, N.
    \label{eq:global-system}
\end{equation}
The matrix $\Aglo$ is symmetric positive definite (SPD) when $\Gamma_D \neq \emptyset$, but $\Aglo$ becomes
increasingly ill-conditioned under mesh refinement and
degree elevation~\cite{gahalaut2019condition, sangalli2016isogeometric},
then suitable preconditioners for Krylov
methods need to be design~\cite{van2003iterative}.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\subsection{The IETI-DP method} \label{subsec:ieti}

Dual-primal Isogeometric Tearing and Interconnecting (IETI-DP) is the
isogeometric counterpart of the FETI-DP family of non-overlapping domain
decomposition methods~\cite{farhat1991method, toselli2004domain}.
The patches of~\eqref{eq:multipatch} serve directly as subdomains, so no
auxiliary mesh partitioning is required.
A defining feature of IETI-DP is that \emph{all dominant computational
work is patch-local and parallelizable}: local stiffness assembly,
Cholesky factorization of local augmented systems, and the per-iteration
back-solves in the scaled Dirichlet preconditioner are each fully independent
across patches \cite{kleiss2012ieti}. Only the coarse problem, of size $\nprim \times \nprim$ with $\nprim = O(K)$
primal dofs, requires global communication.

\paragraph{Local problems and jump operator.}
The discretization for IETI-DP method is define over the non-continuous
space $\Vprod := \prod_{k=1}^{\npatch}
\Vloc{k} \supset \Vglo$. Each patch carries the local stiffness matrix and load vector
\begin{equation}
    (\Aloc{k})_{ij} := \int_{\patchdom{k}} \alpha\, \nabla \Bloc{k}{j} \cdot
    \nabla \Bloc{k}{i} \,\mathrm{d}\Omega, \qquad
    (\floc{k})_{i} := \int_{\patchdom{k}} f \Bloc{k}{i} \,\mathrm{d}\Omega
    + \int_{\Gamma_N \cap \partial\patchdom{k}} g_N \Bloc{k}{i} \,\mathrm{d}s,
    \label{eq:local-stiffness}
\end{equation}
with $i, j = 1, \dots, n_k$ and $k=1, \ldots, \npatch$, where $\{\Bloc{k}{i}\}_{i=1}^{n_k}$ is the basis of
$\Vloc{k}$. Thus $\Aloc{k} \in \R^{n_k \times n_k}$ are SPD matrices
and $\uloc{k} \in \R^{n_k}$ represents $u_k = \sum_i (\uloc{k})_i \Bloc{k}{i}$.
Continuity across patch interfaces is enforced via Lagrange multipliers
$\lagmult \in \R^{\Lambda}$. Introducing the signed Boolean \emph{jump matrices}
$\Jloc{k} \in \R^{\Lambda \times n_k}$ and $\Jfull := (\Jloc{1}, \dots, \Jloc{\npatch})$,
the continuity constraint can be written as $\sum_{k=1}^{\npatch} \Jloc{k} \uloc{k} = \vect{0}$.
Minimizing the broken energy $\sum_k (\tfrac12 \uloc{k}^{\top} \Aloc{k} \uloc{k} -
\floc{k}^{\top} \uloc{k})$ subject to the continuity constraint, with
multiplier $\lagmult \in \R^{\Lambda}$, gives the saddle-point system
\begin{equation}
    \begin{pmatrix}
        \Aloc{1}        &        &                & \Jloc{1}^{\top}        \\
                        & \ddots &                & \vdots                  \\
                        &        & \Aloc{\npatch} & \Jloc{\npatch}^{\top}  \\
        \Jloc{1}        & \cdots & \Jloc{\npatch} & 0
    \end{pmatrix}
    \begin{pmatrix}
        \uloc{1} \\ \vdots \\ \uloc{\npatch} \\ \lagmult
    \end{pmatrix}
    =
    \begin{pmatrix}
        \floc{1} \\ \vdots \\ \floc{\npatch} \\ \vect{0}
    \end{pmatrix}.
    \label{eq:ieti-saddle}
\end{equation}

\paragraph{Primal constraints and Schur complement.}
The blocks $\Aloc{k}$ of floating patches ($\partial\patchdom{k} \cap \Gamma_D =
\emptyset$) are singular. The dual-primal approach remove the singularities selecting some \emph{primal}
degrees of freedom (dof), usually corner values and/or interface averages, stacked as the
rows of a constraint matrix $\Cloc{k} \in \R^{q_k \times n_k}$.
This regularizes the local blocks and introduces a global
coarse subdomain $k = \npatch + 1$:
\begin{equation}
    \Aaug{k} =
    \begin{pmatrix} \Aloc{k} & \Cloc{k}^{\top} \\ \Cloc{k} & 0 \end{pmatrix},
    \quad
    \Jaug{k} = \begin{pmatrix} \Jloc{k} & 0 \end{pmatrix},
    \quad
    \faug{k} = \begin{pmatrix} \floc{k} \\ \vect{0} \end{pmatrix},
    \qquad
    \begin{aligned}
        \Aaug{\npatch+1} &= \textstyle\sum_{k=1}^{\npatch} \Psik{k} \Aloc{k} \Psik{k}^{\top}, \\
        \Jaug{\npatch+1} &= \textstyle\sum_{k=1}^{\npatch} \Jloc{k} \Psik{k}^{\top}, \\
        \faug{\npatch+1} &= \textstyle\sum_{k=1}^{\npatch} \Psik{k} \floc{k}.
    \end{aligned}
    \label{eq:augmented-blocks}
\end{equation}
With $\nprim$ we indicate the number of primal degrees of freedom, and $\Psik{k} \in
\R^{\nprim \times n_k}$ represents the energy-minimizing primal basis functions;
see~\cite{kleiss2012ieti} for the explicit construction. Eliminating the
$\uaug{k}$ leaves an SPD system for the multipliers alone,
\begin{equation}
    \Fschur \lagmult = \gschur,
    \quad
    \Fschur = \sum_{k=1}^{\npatch+1} \Jaug{k} \Aaug{k}^{-1}
    \Jaug{k}^{\top},
    \quad
    \gschur = \sum_{k=1}^{\npatch+1} \Jaug{k} \Aaug{k}^{-1}
    \faug{k}.
    \label{eq:ieti-schur}
\end{equation}
The system is solved by conjugate gradient (CG), and $\uglo$ is
recovered from $\lagmult$ solving the coarse global problem \eqref{eq:augmented-blocks}
and the addressing the local problems in parallel.

\paragraph{Scaled Dirichlet preconditioner.}
The system~\eqref{eq:ieti-schur} is large and ill-conditioned, so CG is
preconditioned by the scaled Dirichlet preconditioner~\cite{kleiss2012ieti,
pechstein2013finite}
\begin{equation}
    \Psd = \sum_{k=1}^{\npatch}
    \Jskel{k}\, \Dmult{k}^{-1}\, \Sloc{k}\, \Dmult{k}^{-1}\, \Jskel{k}^{\top}.
    \label{eq:scaled-dirichlet}
\end{equation}
Here $\Jskel{k}$ is the restriction of $\Jloc{k}$ to the skeleton dofs $\skel_k$
(interfaces and Neumann boundary, complementary to the interior $I_k$).
The diagonal matrix $\Dmult{k}$ is the multiplicity scaling, with
$(\Dmult{k})_{jj} = 1/\mathrm{mult}(j)$ where $\mathrm{mult}(j)$ counts the
number of patches sharing dof $j$.
Finally, $\Sloc{k}$ is the local Dirichlet Schur complement of $\Aloc{k}$ onto
the skeleton. Partitioning $\Aloc{k}$ into interior and skeleton blocks,
\begin{equation}
    \Sloc{k} = A^{(k)}_{\skel\skel}
        - A^{(k)}_{\skel I}\, \bigl(A^{(k)}_{II}\bigr)^{-1}\, A^{(k)}_{I\skel}
    \ \in\ \R^{|\skel_k| \times |\skel_k|}.
    \label{eq:local-schur}
\end{equation}
The overall method is summarized in Algorithm~\ref{alg:ieti-dp}.

\begin{algorithm}[t]
\caption{IETI-DP solver for the model problem~\eqref{eq:poisson-strong}.}
\label{alg:ieti-dp}
\begin{algorithmic}[1]
\Require multipatch $\{\patchdom{k}\}_{k=1}^{\npatch}$, data $(\alpha, f, g_D, g_N)$,
         choice of primal degrees of freedom
\Ensure  discrete continuous approximated solution $u_h \in \Vglo$
\vspace{0.2cm}
\Statex \textbf{Assembly and setup}
\ParFor{k = 1, \dots, \npatch} \Comment{parallel over patches}
    \State assemble local stiffness $\Aloc{k}$ and load $\floc{k}$
           \Comment{Eq.~\eqref{eq:local-stiffness}}
    \State form jump matrix $\Jloc{k}$ and primal constraints $\Cloc{k}$
    \State build augmented blocks $\Aaug{k}, \Jaug{k},
           \faug{k}$ \Comment{Eq.~\eqref{eq:augmented-blocks}}
    \State factorize $\Aaug{k}$ \Comment{one Cholesky per patch; used in all CG steps}
\EndParFor
\State assemble and factorize coarse blocks $\Aaug{\npatch+1},
       \Jaug{\npatch+1}, \faug{\npatch+1}$ \Comment{Eq.~\eqref{eq:augmented-blocks}}
\vspace{0.2cm}
\Statex \textbf{Interface dual problem}
\State $\gschur \gets \sum_{k=1}^{\npatch+1} \Jaug{k}
       \Aaug{k}^{-1} \faug{k}$ \Comment{Eq.~\eqref{eq:ieti-schur}}
\State solve $\Fschur \lagmult = \gschur$ by preconditioned CG, where $\Fschur = \sum_{k=1}^{\npatch+1} \Jaug{k} \Aaug{k}^{-1} \Jaug{k}^{\top}$
\vspace{0.2cm}
\Statex \textbf{Reconstruction}
\State solve the primal problem:
       $\uaug{\npatch+1} \gets \Aaug{\npatch+1}^{-1}
       (\faug{\npatch+1} - \Jaug{\npatch+1}^{\top} \lagmult)$ \Comment{global coarse problem}
\ParFor{k = 1, \dots, \npatch} \Comment{parallel over patches}
    \State $\uaug{k} \gets \Aaug{k}^{-1}
           (\faug{k} - \Jaug{k}^{\top} \lagmult)$
\EndParFor
\State assemble $u_h \in \Vglo$ from $\{\uaug{k}\}_{k=1}^{\npatch}$
\State \Return $u_h$
\end{algorithmic}
\end{algorithm}

\begin{rmk}[Parallel structure and serial cost] \label{rmk:schur-cost}
Each preconditioned CG step applies $\Psd$ via $K$ local Schur-complement
solves (Eq.~\eqref{eq:local-schur}), each requiring one interior back-solve
with the precomputed factorization of $\Aaug{k}$.
These $K$ solves are mutually independent and constitute the dominant
per-iteration cost~\cite[\S4.5]{kleiss2012ieti}.
On a single processor: $O(K p\,n_k^{3/2})$ per CG step.
The ideal case is to have $K$ processors in order to have the
total cost independet from $K$, i.e. $O(p\,n_k^{3/2})$ per CG step.
\end{rmk}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% Section
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{The Neural Schur Preconditioner}
\label{sec:neural-prec}

The per-iteration cost of IETI-DP is dominated by evaluating $\Sloc{k}$ for
every patch in the scaled Dirichlet preconditioner~\eqref{eq:scaled-dirichlet}
(Remark~\ref{rmk:schur-cost}).
We propose to replace each such solve with one evaluation of a learned linear
operator $\Snn(\,\cdot\,;\vect{\zeta}_k)$ conditioned on the patch geometry and
shared across all patches.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\subsection{Formulation}
\label{subsec:formulation}

Let $\vect{\zeta}_k \in \R^{n_k \times (D+1)}$ be the \emph{geometry descriptor}
of patch $k$: the $i$-th entry collects the physical coordinates of the Greville
abscissa $\gmapk{k}(\vect{\eta}^{\,\mathrm{Gr}}_i) \in \R^D$ \cite{cottrell2009isogeometric}, and its NURBS weight $w_i$,
\begin{equation}
    \vect{\zeta}_k
    = \bigl\{ \bigl(\gmapk{k}(\vect{\eta}^{\,\mathrm{Gr}}_i),\, w_i\bigr)
      \bigr\}_{i=1}^{n_k}.
    \label{eq:geom-features}
\end{equation}
We define the \emph{neural Schur operator} with shared weights $\theta$ as
\begin{equation}
    \Snn\bigl(\,\cdot\,;\, \vect{\zeta}_k\bigr)
    : \R^{|\skel_k|} \to \R^{|\skel_k|},
    \qquad
    \vect{v} \mapsto \Snn(\vect{v}; \vect{\zeta}_k) \approx \Sloc{k} \vect{v},
    \label{eq:neural-schur}
\end{equation}
and the \emph{neural scaled Dirichlet preconditioner} by substituting \eqref{eq:neural-schur}
into \eqref{eq:scaled-dirichlet}:
\begin{equation}
    \Pneural = \sum_{k=1}^{\npatch}
    \Jskel{k}\, \Dmult{k}^{-1}\, \Snn(\,\cdot\,;\vect{\zeta}_k)\,
    \Dmult{k}^{-1}\, \Jskel{k}^{\top}.
    \label{eq:neural-prec}
\end{equation}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\subsection{Architecture}
\label{subsec:architecture}

The network takes only $\vect{\zeta}_k$ as input and outputs a matrix
$M_\theta(\vect{\zeta}_k) \in \R^{|\skel_k| \times |\skel_k|}$\mg{dipendenza da k, capire come gestirla, se fare proiezione e prolungamento o supporre che siano tutte uguali tra loro}, so that
\begin{equation}
    \Snn(\vect{v};\,\vect{\zeta}_k) = M_\theta(\vect{\zeta}_k)\,\vect{v}.
    \label{eq:matrix-form}
\end{equation}
Linearity in $\vect{v}$ is immediate since $\vect{v}$ is not an input to the
network.
$M_\theta(\vect{\zeta}_k)$ is parameterized in the SPD factorized form, see [CITE]\mg{fix citation once available}, as
\begin{equation}
    M_\theta(\vect{\zeta}_k)
    = \frac{1}{\sqrt{\dv}}\,
      Q_\theta(\vect{\zeta}_k)\,
      Q_\theta(\vect{\zeta}_k)^{\top}
      + \varepsilon_\theta\, I,
    \label{eq:spd-param}
\end{equation}
where $Q_\theta(\vect{\zeta}_k) \in \R^{|\skel_k| \times \dv}$, $\dv$ is the
latent dimension, and $\varepsilon_\theta > 0$ is a learnable scalar.
The matrix $M_\theta(\vect{\zeta}_k)$ is SPD
for any $Q_\theta(\vect{\zeta}_k)$ and $\varepsilon_\theta > 0$.
Applying~\eqref{eq:spd-param} costs $O(|\skel_k|\dv)$ via two matrix--vector
products.

The factor $Q_\theta(\vect{\zeta}_k)$ is computed by the IgaNet Transformer
encoder, the entries of $\vect{\zeta}_k$ are treated as
a sequence of $n_k$ tokens in $\R^{D+1}$, processed through a learnable affine
embedding plus sinusoidal positional encoding,
followed by $L$ layers of multi-head self-attention~\cite{vaswani2017attention}.
A cross-attention resampling step then maps the encoded sequence to
$G_\theta(\vect{\zeta}_k) \in \R^{|\skel_k| \times \dv}$ using $|\skel_k|$ learnable target
queries, and a final linear projection gives $Q_\theta(\vect{\zeta}_k) = G_\theta(\vect{\zeta}_k) W_Q$.
We refer to [CITE] for the complete architecture details.

\begin{rmk}[PCG compatibility]
\label{rmk:linearity}
Since the preconditioner $\Pneural$ in~\eqref{eq:neural-prec} is a linear SPD preconditioner, it is compatible with the standard PCG without modifications. Furthermore we can adopt our preconditioner in the CG solver of the lagrange multiplier system~\eqref{eq:ieti-schur} without adopting flexible variants.
\end{rmk}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\subsection{Training}
\label{subsec:training}

\paragraph{Dataset.}
For each patch geometry in a training collection, $N_{\mathrm{data}} = 20\,000$
probe vectors $\vect{v}^{(i)} \in \R^{|\skel_k|}$ are drawn from a Gaussian
random field (GRF) on the skeleton~\cite{adler2010geometry} with exponential
covariance
\begin{equation}
    \mathrm{Cov}\bigl(\vect{v}(\vect{x}),\, \vect{v}(\vect{y})\bigr)
    = \sigma^2 \exp\!\Bigl(-\frac{\|\vect{x}-\vect{y}\|}{\lambda}\Bigr),
    \quad \lambda = 2d^2,\; d = 0.05,\; \sigma = 1,
    \label{eq:grf-cov}
\end{equation}
following~\cite{kopanivcakova2025deeponet}.
The probe vectors are not network inputs; they evaluate the loss.
Targets $\Sloc{k}\vect{v}^{(i)}$ are computed from the local factorizations in
Algorithm~\ref{alg:ieti-dp}.
The dataset is split into training (80\%), validation (10\%), and test (10\%) sets.

\paragraph{Loss.}
\begin{equation}
    \mathrm{Loss}
    = \frac{1}{N_{\mathrm{train}}}
      \sum_{i=1}^{N_{\mathrm{train}}}
      \Bigl(\frac{1}{|\skel_k|}
        \bigl\|M_\theta(\vect{\zeta}_k)\,\vect{v}^{(i)}
              - \Sloc{k}\vect{v}^{(i)}
        \bigr\|^2_2
      \Bigr)^{1/2}.
    \label{eq:loss}
\end{equation}

\paragraph{Optimizer and hyperparameters.}
Training uses AdamW~\cite{loshchilov2019decoupledweightdecayregularization} for
$500$ epochs (batch size $100$) with step learning-rate decay.
Hyperparameters are selected by HyperNOs~\cite{ghiotto2025hypernos} via
TPE~\cite{watanabe2023tree,bergstra2015hyperopt} and
ASHA~\cite{li2020massivelyparallelhyperparametertuning} over $200$ trials.
The search space is:
\begin{center}
\begin{tabular}{ll}
    \toprule
    Hyperparameter & Range \\
    \midrule
    Learning rate $\lambda$ & $[10^{-4}, 10^{-2}]$ \\
    LR reduction factor $\eta$ & $[0.75, 0.99]$ \\
    Weight decay $w$ & $[10^{-6}, 10^{-4}]$ \\
    Dropout~\cite{srivastava2014dropout} $\gamma$ & $[0.00, 0.10]$ \\
    Encoder layers $L$ & $\{1, \dots, 5\}$ \\
    Encoder heads $n_h$ & divisors of $\dv$ \\
    Output heads $H$ & $\{1, 3\}$ \\
    Attention activation $\sigma$ & $\{\mathrm{ReLU}, \mathrm{GELU}, \mathrm{Softmax}\}$ \\
    \bottomrule
\end{tabular}
\end{center}
The latent dimension $\dv$ and output heads $H$ are fixed per configuration;
optimal hyperparameters and training results are reported in
Section~\ref{sec:reference-benchmark}.


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% Section
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Reference benchmark}
\label{sec:reference-benchmark}

All experiments run on a 12-core machine with shared-memory parallelism,
stopping tolerance $10^{-8}$, and maximum $1000$ iterations.\mg{State CPU model.}
Timings are the mean over five repetitions.

\subsection{Setup} \label{subsec:bench-setup}

We compare Direct Cholesky; GMRES and CG with no preconditioner, Jacobi, and symmetric Gauss--Seidel;
geometric multigrid as a standalone solver; and the IETI-DP solver of
Section~\ref{subsec:ieti} (with and without the scaled Dirichlet preconditioner).
The geometric multigrid uses Gauss--Seidel smoothing (one pre-/post-smooth step,
V-cycle; levels equal to the number of $h$-refinement steps)
Geometry, basis, boundary
conditions, and right-hand side are identical across all methods.
Two test geometries are considered: a planar B-spline multipatch domain in $\R^2$
(Section~\ref{subsec:bench-2d}) and an open NURBS surface in $\R^3$
(Section~\ref{subsec:bench-3d}).
Each sweep varies one of three parameters: the number of uniform patch splits
($\times 2^d$ subdomains per split), the local polynomial degree $p$, or the
uniform $h$-refinement level.


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\subsection{Theoretical convergence rates}
\label{subsec:theory}

We collect the asymptotic estimates against which the parameter studies of
Sections~\ref{subsec:bench-2d}--\ref{subsec:bench-3d} are compared.
Throughout, $N$ is the number of coupled DOFs, $K$ the number of patches,
$n_k = N/K$ the average local patch size, $h\sim n_k^{-1/2}$ the local mesh
size, $H$ the subdomain diameter, and $p$ the spline degree.
Table~\ref{tab:theory} summarises the predicted serial and parallel
($K$ processors, one per patch) costs for the refinement sweep
(fixed $K$, $p$); the text below justifies each entry.

\begin{table}[h]
  \centering
  \caption{Predicted asymptotic scaling for the refinement sweep.
    $\kappa$: condition number; ``iters'': count to tolerance $\varepsilon$;
    ``serial'': total cost on one processor; ``parallel'': wall-clock cost on
    $K$ processors (one per patch). $p$-dependence absorbed into constants.
    Only IETI-DP is inherently patch-parallel; the remaining solvers act on the assembled global
    system and run serially.}
  \label{tab:theory}
  \begin{tabular}{lcccc}
    \toprule
    Method                         & $\kappa$            & iters              & serial                          & parallel ($K$ procs)     \\
    \midrule
    Direct (Cholesky)              & ---                 & ---                & $O(K^{3/2}n_k^{3/2})$          & ---                      \\
    GMRES (no preconditioner)         & $O(n_k)$            & $O(n_k^{1/2})$     & $O(K\,n_k^{3/2})$              & ---    \\
    GMRES + Jacobi/ symm.\ GS                & $O(n_k)$            & $O(n_k^{1/2})$     & $O(K\,n_k^{3/2})$              & ---    \\
    CG (no preconditioner)         & $O(n_k)$            & $O(n_k^{1/2})$     & $O(K\,n_k^{3/2})$              & ---    \\
    CG + Jacobi/ symm.\ GS                & $O(n_k)$            & $O(n_k^{1/2})$     & $O(K\,n_k^{3/2})$              & ---    \\
    Multigrid (standalone)         & $O(1)$              & $O(1)$             & $O(K\,n_k)$                    & ---                      \\
    IETI-DP                        & $O(\log^2 n_k)$     & $O(\log n_k)$      & $O(K\,n_k^{3/2}\!\log n_k)$   & $O(n_k^{3/2}\!\log n_k)$ \\
    % IETI-DP${}^{\dagger}$          & $O(\log^2 n_k)$     & $O(\log n_k)$      & $O(K\,n_k^{1/2}\!\log n_k)$   & $O(n_k^{1/2}\!\log n_k)$ \\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{Direct solver.}
For a 2-D elliptic problem on a quasi-uniform mesh, nested
dissection~\cite[Thm.~4.1]{george1973nested} produces a Cholesky factorization in
$O(N^{3/2}) = O(K^{3/2}n_k^{3/2})$ operations.
For IGA with tensor-product B-splines of degree $p$, each basis function
couples $O(p^d)$ neighbours; the nested-dissection separators grow by a
factor $p$, raising the factorization cost to $O(p^3 N^{3/2})$ in 2D.

\paragraph{Conjugate gradient.}
The stiffness matrix condition number satisfies~\cite[Thm.~4.3]{gahalaut2019condition}
\begin{equation}
  \kappa(A) \;\sim\; h^{-2} \;\sim\; \frac{N}{K}.
  \label{eq:kappa-cg}
\end{equation}
Denoting the error at step $k$ by $e_k = x_*-x_k$ and the
energy norm by $\|v\|_A=(Av,v)^{1/2}$, the CG convergence
bound~\cite[Thm.~6.29]{saad2003iterative} gives
$\|e_k\|_{A} \le 2\!\left(\tfrac{\sqrt{\kappa(A)}-1}{\sqrt{\kappa(A)}+1}\right)^{\!k}\|e_0\|_{A}$,
the number of iterations to reach tolerance $\varepsilon$ scales as
$O\!\left(\sqrt{\kappa(A)}\log(1/\varepsilon)\right)\sim O\!\left(n_k^{1/2}\right)$
and the total serial solve cost as $O\!\left(K n_k^{3/2}\right)$.
For degree elevation at fixed $h$ the condition number grows
super-algebraically: $\kappa(A)\leq C\,p^{2d+2}4^{pd}$~\cite[Thm.~4.8]{gahalaut2019condition},
making unpreconditioned CG rapidly intractable as $p$ increases.
\\
Jacobi and symmetric Gauss--Seidel preconditioners reduce the spectral radius but leave the asymptotic order
of the condition number unchanged:
$\kappa(M_{\mathrm{J}}^{-1}A) = O(h^{-2})$ and
$\kappa(M_{\mathrm{GS}}^{-1}A) = O(h^{-2})$.
The iteration count therefore retains the same $O\!\left((N/K)^{1/2}\right)$
scaling with an improved (but not asymptotically better) constant.

\paragraph{GMRES.}
For the SPD systems arising from~\eqref{eq:poisson-strong}, the convergence
bound~\cite[Cor.~6.33]{saad2003iterative} reduces to the CG bound~\cite[Thm.~6.29]{saad2003iterative},
giving the same $O\!\left((N/K)^{1/2}\right)$ iteration count and $O\!\left(N^{3/2}/K^{1/2}\right)$
total cost. The condition number and degree scaling are identical to the CG case.

\paragraph{Multigrid.}\mg{Check this ...}
Under standard elliptic regularity a $V$-cycle with one pre- and one
post-smoothing step yields a mesh-independent contraction factor, giving
$\kappa(M_{\mathrm{MG}}^{-1}A)=O(1)$ and hence $O(1)$ CG
iterations~\cite[Ch.~7]{hackbusch1994iterative},~\cite[\S2.3]{trottenberg2000multigrid}.
The total solve cost is therefore $O(N)$, which is optimal for this class of
problem.
This estimate requires standard elliptic regularity on a flat (Euclidean)
domain; for curved manifolds the V-cycle contraction factor need not be
mesh-independent, and iteration counts may grow with $N$ (see
Section~\ref{subsubsec:bench-3d-sweeps}).
This $h$-robustness does \emph{not} carry over to degree elevation:
standard point smoothers are not $p$-robust in IGA and the iteration count
grows with $p$~\cite[\S5]{tielen2020pmultigrid}.

\paragraph{IETI-DP with scaled Dirichlet preconditioner.}
The spectral bound for the preconditioned interface
system~\eqref{eq:ieti-schur}--\eqref{eq:scaled-dirichlet}
\cite{schneckenleitner2020condition} is:
\begin{equation}
  \kappa\!\left(M_{sD}^{-1}F\right)
  \;\leq\; C\,p\,\bigl(1+\log p+\log(H/h)\bigr)^{2},
  \label{eq:kappa-ieti}
\end{equation}
where $C>0$ is independent of $h$, $H$, $K$, $N$, and $p$.
By the CG convergence bound~\cite[Thm.~6.29]{saad2003iterative}, the number of iterations to
reach tolerance $\varepsilon$ scales as
$O\!\left(\sqrt{\kappa(M_{sD}^{-1}F)}\,\log(1/\varepsilon)\right)$,
and the dominant per-iteration cost is $O(K\,p\,n_k^{3/2})$ in serial
and $O(p\,n_k^{3/2})$ on $K$ parallel processors (Remark~\ref{rmk:schur-cost}).
Three consequences govern the parameter sweeps.
\begin{itemize}
  \item[(i)] \textbf{Refinement} ($K$ fixed): $H/h\sim(N/K)^{1/2}$
    gives $\kappa=O(\log^{2}n_k)$ and hence $O\!\left(\sqrt{\kappa}\right)=O(\log n_k)$ CG iterations.
  \item[(ii)] \textbf{Subdomain} ($h,p$ fixed per patch): $H/h$ stays
    roughly constant as $K$ grows, so~\eqref{eq:kappa-ieti} predicts
    near-constant conditioning.%; in practice iteration counts grow
    % moderately (Sections~\ref{subsubsec:bench-2d-sweeps}--\ref{subsubsec:bench-3d-sweeps}).
  \item[(iii)] \textbf{Degree} ($h$ fixed):
    $\kappa=O\!\left(p\,(1+\log p)^{2}\right)$ \cite{schneckenleitner2020condition},
    giving $O\!\left(\sqrt{p}\,(1+\log p)\right)$ CG iterations.
    % , this is algebraic growth
    % that is far milder than the super-algebraic $\kappa(A_h)\leq C\,p^{2d+2}4^{pd}$
    % for unpreconditioned CG.
\end{itemize}

After solving the system~\eqref{eq:ieti-schur}, the reconstruction step (Algorithm~\ref{alg:ieti-dp}, lines 10--15)
recovers each $\uaug{k}$ by one back-solve with the precomputed $\Aaug{k}$
factorization, costing $O(K\,p\,n_k^{3/2})$ in serial or $O(p\,n_k^{3/2})$
in parallel. This is equivalent to a single CG iteration, so the total solve cost is driven by 
the cost of the resolution of the interface problem~\eqref{eq:ieti-schur}.

% \noindent\emph{Condition number verification.}
% The yeti refinement sweep provides a clean quantitative test ($K=21$ fixed):
% unpreconditioned CG iterations increase from $118$ at $r=2$ ($N=5\,760$) to
% $864$ at $r=5$ ($N=347\,136$), a factor of $7.3\times$, consistent with the
% predicted $\sqrt{N_5/N_2}=\sqrt{60.3}=7.8\times$ from~\eqref{eq:kappa-cg} ($\kappa\sim N/K$).
% IETI-DP iterations grow from $14$ at $r=2$ to $17$ at $r=4$: the
% bound~\eqref{eq:kappa-ieti} predicts a ratio
% $\sqrt{(1+\tfrac{1}{2}\ln(N_4/K))^{2}/(1+\tfrac{1}{2}\ln(N_2/K))^{2}}
% = (5.17/3.81) = 1.36\times$ at most; the observed ratio is $17/14=1.21\times$,
% consistent with the bound.
% For the teapot ($K=32$ fixed), IETI-DP iterations grow from $19$ at $r=2$
% ($N=750$) to $38$ at $r=6$ ($N=134\,550$): the bound predicts
% $(1+\ln\sqrt{N_6/K})/(1+\ln\sqrt{N_2/K})=(5.17/2.58)=2.00\times$ growth at
% most; the observed factor is $38/19=2.00\times$, confirming the bound is
% tight in this case.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\subsection{Two-dimensional benchmark: yeti domain}
\label{subsec:bench-2d}

The yeti domain is a planar multiply-connected B-spline multipatch domain with
$\npatch = 21$ patches (Figure~\ref{fig:yeti-refcase}), all sides Dirichlet.

\begin{figure}[!ht]
    \centering
    \begin{subfigure}[b]{0.27\textwidth}
        \includegraphics[width=\textwidth]{figures/yeti_geom.pdf}
        \caption{Multipatch discretization}
        \label{fig:yeti-refcase-mesh}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.32\textwidth}
        \includegraphics[width=\textwidth]{figures/yeti_rhs.pdf}
        \caption{Right-hand side}
        \label{fig:yeti-refcase-rhs}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.32\textwidth}
        \includegraphics[width=\textwidth]{figures/yeti_sol.pdf}
        \caption{Computed solution}
        \label{fig:yeti-refcase-sol}
    \end{subfigure}
    \caption{Reference case (yeti domain, $21$ patches, $p = 2$, $r = 3$):
    the multipatch discretization, the source term, and the IETI solution.}
    \label{fig:yeti-refcase}
\end{figure}

\subsubsection{Reference case} \label{subsubsec:bench-2d-reference}

The reference case uses $p = 2$, $r = 4$: $N = 87\,552$ dofs, $\Lambda = 1536$
Lagrange multipliers. Every patch corner is a Dirichlet node, so $\nprim = 0$
and IETI-DP reduces to plain IETI. Table~\ref{tab:yeti-reference} separates
setup (one-time local Cholesky factorization) from solve (Krylov iterations).
IETI-DP has the cheapest solve at $0.12\,$s ($5.8\times$
faster than multigrid standalone at $0.69\,$s), but an expensive setup ($2.44\,$s,
the $K=21$ local Cholesky factorizations) that requires approximately $4$
right-hand sides to amortize over multigrid standalone.
The parameter-study figures report solve-only time, corresponding to the
multi-RHS regime for which IETI-DP is designed; in a parallel setting the
setup cost would also reduce proportionally.

\begin{table}[t]
    \centering
    \caption{Reference case (yeti domain, $\npatch = 21$, $p = 2$, $r = 4$;
    $N = 87\,552$). Setup is measured once; solve times are means over 5 runs.
    Tolerance $10^{-8}$.
    IETI-DP iteration counts are CG steps on the interface
    problem~\eqref{eq:ieti-schur} and are not directly comparable with the
    global-system counts.}
    \label{tab:yeti-reference}
    \begin{tabular}{lcccc}
        \toprule
        Method & setup [s] & solve [s] & iters & converged \\
        \midrule
        Direct Cholesky                 & 0.13 & 0.47  & ---  & yes \\
        GMRES (no preconditioner)       & 0.13 & 4.85  & 397  & yes \\
        GMRES + Jacobi                  & 0.13 & 2.58  & 280  & yes \\
        GMRES + symm. Gauss--Seidel & 0.13 & 4.57  & 100  & yes \\
        CG (no preconditioner)          & 0.13 & 0.47  & 446  & yes \\
        CG + Jacobi                     & 0.13 & 0.46  & 319  & yes \\
        CG + symm. Gauss--Seidel    & 0.13 & 3.23  & 104  & yes \\
        Multigrid (standalone)          & 0.21 & 0.69  & 18   & yes \\
        IETI-DP (no prec)               & 2.44 & ---   & ---  & --- \\
        \textbf{IETI-DP}                & 2.44 & \textbf{0.12} & 17 & yes \\
        \bottomrule
    \end{tabular}
\end{table}

\subsubsection{Parameter studies} \label{subsubsec:bench-2d-sweeps}

Figure~\ref{fig:yeti-sweeps} shows solve times and iteration counts as each
discretization parameter is varied around the reference case.
All measurements are single-processor; the parallel advantage of IETI-DP
(Remark~\ref{rmk:schur-cost}) is not reflected in these serial timings.

\begin{figure}[!ht]
    \centering
    \begin{subfigure}[b]{0.46\textwidth}
        \includegraphics[width=\textwidth]{figures/yeti_time_vs_refinement.pdf}
        \caption{Solve time, varying $r$ ($\npatch=21$, $p=2$).}
        \label{fig:yeti-time-r}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.46\textwidth}
        \includegraphics[width=\textwidth]{figures/yeti_iters_vs_refinement.pdf}
        \caption{Iterations, varying $r$ ($\npatch=21$, $p=2$).}
        \label{fig:yeti-iters-r}
    \end{subfigure}
    \par
    \begin{subfigure}[b]{0.46\textwidth}
        \includegraphics[width=\textwidth]{figures/yeti_time_vs_degree.pdf}
        \caption{Solve time, varying $p$ ($\npatch=21$, $r=2$).}
        \label{fig:yeti-time-p}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.46\textwidth}
        \includegraphics[width=\textwidth]{figures/yeti_iters_vs_degree.pdf}
        \caption{Iterations, varying $p$ ($\npatch=21$, $r=2$).}
        \label{fig:yeti-iters-p}
    \end{subfigure}
    \par
    \begin{subfigure}[b]{0.46\textwidth}
        \includegraphics[width=\textwidth]{figures/yeti_time_vs_splitpatches.pdf}
        \caption{Solve time, varying $K$ ($p=2$, $r=2$).}
        \label{fig:yeti-time-K}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.46\textwidth}
        \includegraphics[width=\textwidth]{figures/yeti_iters_vs_splitpatches.pdf}
        \caption{Iterations, varying $K$ ($p=2$, $r=2$).}
        \label{fig:yeti-iters-K}
    \end{subfigure}
    \caption{Yeti domain: solve time (left) and iteration count (right) as each
    discretization parameter is varied around the reference case ($\npatch = 21$,
    $p = 2$, $r = 2$).}
    \label{fig:yeti-sweeps}
\end{figure}

\emph{(i)~Mesh resolution} (Figures~\ref{fig:yeti-time-r},
\ref{fig:yeti-iters-r}):
IETI-DP leads at every refinement level, with roughly $6\times$ advantage
over multigrid at $r=4$ ($0.12\,$s vs.\ $0.69\,$s),
narrowing to $4.5\times$ at $r=5$ ($N=347\,136$).
Multigrid (standalone) iteration counts remain nearly flat at $16$--$20$
(Figure~\ref{fig:yeti-iters-r}), confirming approximate $h$-robustness on
the flat domain.
IETI-DP iterations grow mildly from $14$ at $r=2$ to $18$ at $r=5$, well
within the $O(\log N)$ bound~\eqref{eq:kappa-ieti} (see condition number
verification in Section~\ref{subsec:theory}).
The narrowing advantage at $r=5$ reflects the asymptotic dominance of
IETI-DP's $O(N^{3/2}/K^{1/2})$ per-iteration cost over multigrid's $O(N)$.

\emph{(ii)~Polynomial degree} (Figures~\ref{fig:yeti-time-p},
\ref{fig:yeti-iters-p}):
IETI-DP leads at every $p$, with the gap growing from $7.4\times$ at $p=1$
to $440\times$ at $p=6$ ($0.009\,$s vs.\ $4.06\,$s).
The widening is driven entirely by multigrid smoother deterioration: multigrid
(standalone) iterations increase from $20$ to $488$, while IETI-DP iterations
grow from $13$ to only $18$---roughly one additional iteration per unit increase
in $p$, far milder than the $O(p^{3/2})$ that the $O(p^3)$ bound on $C$
in~\eqref{eq:kappa-ieti} would imply~\cite{kleiss2012ieti}.
Note that at $p=6$, CG (no preconditioner) also fails to converge within $1000$
iterations on this flat domain.

\emph{(iii)~Number of subdomains} (Figures~\ref{fig:yeti-time-K},
\ref{fig:yeti-iters-K}):
IETI-DP leads for $K\leq 336$ ($10.8\times$ at $K=21$, $1.7\times$ at $K=336$)
and falls behind at $K=1344$ (multigrid $1.7\times$ faster) and $K=5376$
(multigrid $3.9\times$ faster, $0.98\,$s vs.\ $3.85\,$s).
Multigrid (standalone) iteration counts remain essentially constant at $16$--$27$
across all split levels, confirming $K$-independence of the condition number on
this flat geometry.
IETI-DP iterations grow from $14$ to $29$; as explained in
Section~\ref{subsec:theory}~(ii), the spectral bound~\eqref{eq:kappa-ieti}
predicts roughly constant conditioning (since $H/h\approx\sqrt{N/K}\approx 5$
throughout), so the cost increase is primarily algebraic---each CG step
requires $K$ local back-solves and the Lagrange multiplier system scales
with $K$.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\subsection{Three-dimensional benchmark: teapot surface}
\label{subsec:bench-3d}

The teapot is an open NURBS surface in $\R^3$ with $\npatch = 32$ patches
(Figure~\ref{fig:refcase}). The physical domain is a curved manifold ($d = 2 < D = 3$),
so the assembled operator is the Laplace--Beltrami operator and the Jacobian is
non-square (Moore--Penrose pseudo-inverse used for physical gradients). The field
$u = \sin(x)\cos(y)$ is no longer exact, so the $L^2$ column measures
inter-method agreement, not solution accuracy.

\begin{figure}[t]
    \centering
    \begin{subfigure}[b]{0.32\textwidth}
        \includegraphics[width=\textwidth]{figures/teapot_geom.png}
        \caption{Multipatch discretization}
        \label{fig:refcase-mesh}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.32\textwidth}
        \includegraphics[width=\textwidth]{figures/teapot_rhs.png}
        \caption{Right-hand side}
        \label{fig:refcase-rhs}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.32\textwidth}
        \includegraphics[width=\textwidth]{figures/teapot_sol.png}
        \caption{Computed solution}
        \label{fig:refcase-sol}
    \end{subfigure}
    \caption{Reference case (teapot surface, $32$ patches, $p = 2$, $r = 3$):
    the multipatch discretization, the source term, and the IETI-DP solution.}
    \label{fig:refcase}
\end{figure}

\subsubsection{Reference case} \label{subsubsec:bench-3d-reference}

The reference case uses $p = 2$, $r = 4$: $N = 9\,078$ dofs, $\Lambda = 1024$
Lagrange multipliers, $\nprim = 22$ primal dofs (corner constraints).
Table~\ref{tab:reference} separates setup from solve.
IETI-DP has the fastest solve ($0.010\,$s), $5.3\times$ faster than the next best
converging iterative method (CG+Jacobi at $0.053\,$s) and $1.8\times$ faster than
Direct Cholesky ($0.018\,$s). Multigrid (standalone) and CG (no preconditioner) do
not converge within 1000 iterations on this curved surface.
The setup cost ($0.095\,$s) is amortized within approximately $2$ right-hand
sides over CG+Jacobi (and $10$ over Direct Cholesky), making IETI-DP the
preferred iterative solver for the multi-RHS regime.

\begin{table}[t]
    \centering
    \caption{Reference case (teapot, $\npatch = 32$, $p = 2$, $r = 4$;
    $N = 9\,078$). Setup is measured once; solve times are means over 5 runs.
    Tolerance $10^{-8}$.
    IETI-DP iterations count CG steps on the interface
    problem~\eqref{eq:ieti-schur}, hence are not directly comparable with the
    global-system iteration counts.}
    \label{tab:reference}
    \begin{tabular}{lcccc}
        \toprule
        Method & setup [s] & solve [s] & iters & converged \\
        \midrule
        Direct Cholesky                 & 0.015 & 0.018  & ---  & yes \\
        GMRES (no preconditioner)       & 0.015 & 1.20   & 452  & yes \\
        GMRES + Jacobi                  & 0.015 & 0.39   & 296  & yes \\
        GMRES + symm. Gauss--Seidel & 0.015 & 0.21   & 134  & yes \\
        CG (no preconditioner)          & 0.015 & 0.10   & 1000 & no  \\
        CG + Jacobi                     & 0.015 & 0.053  & 381  & yes \\
        CG + symm. Gauss--Seidel    & 0.015 & 0.16   & 144  & yes \\
        Multigrid (standalone)          & 0.022 & 1.99   & 1000 & no  \\
        IETI-DP (no prec)               & 0.095 & ---    & ---  & --- \\
        \textbf{IETI-DP}                & 0.095 & \textbf{0.010} & 26 & yes \\
        \bottomrule
    \end{tabular}
\end{table}

\subsubsection{Parameter studies} \label{subsubsec:bench-3d-sweeps}

Figure~\ref{fig:teapot-sweeps} records solve times and iteration counts as each
parameter is varied (all single-processor).
The solve phase is the target of the neural
operator~\eqref{eq:neural-schur}, which replaces one Schur-complement action
per PCG iteration.

\begin{figure}[!ht]
    \centering
    \begin{subfigure}[b]{0.46\textwidth}
        \includegraphics[width=\textwidth]{figures/time_vs_refinement.pdf}
        \caption{Solve time, varying $r$ ($\npatch=32$, $p=2$).}
        \label{fig:teapot-time-r}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.46\textwidth}
        \includegraphics[width=\textwidth]{figures/iters_vs_refinement.pdf}
        \caption{Iterations, varying $r$ ($\npatch=32$, $p=2$).}
        \label{fig:teapot-iters-r}
    \end{subfigure}
    \par
    \begin{subfigure}[b]{0.46\textwidth}
        \includegraphics[width=\textwidth]{figures/time_vs_degree.pdf}
        \caption{Solve time, varying $p$ ($\npatch=32$, $r=2$).}
        \label{fig:teapot-time-p}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.46\textwidth}
        \includegraphics[width=\textwidth]{figures/iters_vs_degree.pdf}
        \caption{Iterations, varying $p$ ($\npatch=32$, $r=2$).}
        \label{fig:teapot-iters-p}
    \end{subfigure}
    \par
    \begin{subfigure}[b]{0.46\textwidth}
        \includegraphics[width=\textwidth]{figures/time_vs_splitpatches.pdf}
        \caption{Solve time, varying $K$ ($p=2$, $r=2$).}
        \label{fig:teapot-time-K}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.46\textwidth}
        \includegraphics[width=\textwidth]{figures/iters_vs_splitpatches.pdf}
        \caption{Iterations, varying $K$ ($p=2$, $r=2$).}
        \label{fig:teapot-iters-K}
    \end{subfigure}
    \caption{Teapot surface: solve time (left) and iteration count (right) as each
    discretization parameter is varied around the reference case ($\npatch = 32$,
    $p = 2$, $r = 2$).}
    \label{fig:teapot-sweeps}
\end{figure}

\emph{(i)~Mesh resolution} (Figures~\ref{fig:teapot-time-r},
\ref{fig:teapot-iters-r}):
IETI-DP leads at every refinement level, reaching $8.75\times$ at $r=2$
($0.0004\,$s vs.\ $0.0035\,$s).
A key distinction from the flat yeti domain: multigrid (standalone) converges
only at $r=2$--$3$ (respectively $8.75\times$ and $4.8\times$ slower than
IETI-DP) and fails to converge within 1000 iterations from $r=4$ onwards.
The Laplace--Beltrami operator on the curved teapot surface degrades the
V-cycle contraction factor, leading to multigrid divergence rather than merely
slow convergence---in stark contrast to the flat-domain $O(1)$ prediction of
Section~\ref{subsec:theory}.
IETI-DP instead converges robustly at every level, with iterations growing from
$19$ at $r=2$ to $38$ at $r=6$, consistent with~\eqref{eq:kappa-ieti} (see the
condition number verification in Section~\ref{subsec:theory}).

\emph{(ii)~Polynomial degree} (Figures~\ref{fig:teapot-time-p},
\ref{fig:teapot-iters-p}):
IETI-DP leads at every $p$; the gap grows from $4.5\times$ at $p=1$
($0.0002\,$s vs.\ $0.0009\,$s) to over $594\times$ at $p=6$ ($0.002\,$s
vs.\ $1.31\,$s), with multigrid failing to converge at $p=6$.
Multigrid (standalone) iterations increase from $24$ at $p=1$ to $1000$ at
$p=6$ (diverging), while IETI-DP iterations grow from $16$ to only $25$
(Figure~\ref{fig:teapot-iters-p}), confirming that multigrid smoother
deterioration---not the IETI interface problem---drives the widening cost gap.

\emph{(iii)~Number of subdomains} (Figures~\ref{fig:teapot-time-K},
\ref{fig:teapot-iters-K}):
Multigrid (standalone) converges for $K\leq 512$, with IETI-DP $8.75\times$
faster at $K=32$ and $2.8\times$ at $K=512$; it fails to converge within
1000 iterations for $K\geq 2048$ on this curved surface.
IETI-DP iteration counts grow from $19$ at $K=32$ to $125$ at $K=8192$.
At $K\geq 2048$, multigrid, unpreconditioned CG and GMRES also diverge;
among iterative solvers, IETI-DP remains fastest (followed by CG and GMRES
with symmetric Gauss-Seidel), while the increasing Lagrange multiplier count
drives up the per-iteration cost.
The $O(\log N)$ spectral bound~\eqref{eq:kappa-ieti} limits the iteration
growth despite the large number of subdomains.



%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% Bibliography (Harvard notation)
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\clearpage
\begin{thebibliography}{99}
    \bibitem{george1973nested}
    George, J.A., 1973. Nested dissection of a regular finite element mesh. SIAM Journal on Numerical Analysis, 10(2), pp.345--363.

    \bibitem{hackbusch1994iterative}
    Hackbusch, W., 1994. Iterative solution of large sparse systems of equations. Springer-Verlag, New York.

    \bibitem{saad2003iterative}
    Saad, Y., 2003. Iterative methods for sparse linear systems (2nd edn.). SIAM, Philadelphia.

    \bibitem{van2003iterative}
    Van der Vorst, H.A., 2003. Iterative Krylov methods for large linear systems (No. 13). Cambridge University Press.

    \bibitem{kleiss2012ieti}
    Kleiss, S.K., Pechstein, C., Jüttler, B. and Tomar, S., 2012. IETI–isogeometric tearing and interconnecting. Computer methods in applied mechanics and engineering, 247, pp.201-215.

    \bibitem{schneckenleitner2020condition}
    Schneckenleitner, R. and Takacs, S., 2020. Condition number bounds for IETI-DP methods that are explicit in $h$ and $p$. Mathematical Models and Methods in Applied Sciences, 30(11), pp.2067--2103.

    \bibitem{farhat1991method}
    Farhat, C. and Roux, F.X., 1991. A method of finite element tearing and interconnecting and its parallel solution algorithm. International Journal for Numerical Methods in Engineering, 32(6), pp.1205-1227.

    \bibitem{hofer2018dual}
    Hofer, C. and Langer, U., 2017. Dual-primal isogeometric tearing and interconnecting solvers for multipatch dG-IgA equations. Computer Methods in Applied Mechanics and Engineering, 316, pp.2-21.

    \bibitem{pechstein2013finite}
    Pechstein, C., 2013. Finite and boundary element tearing and interconnecting solvers for multiscale problems. Lecture Notes in Computational Science and Engineering, Vol. 90. Springer.

    \bibitem{wu2026dlhim}
    Wu, Y., van Beek, J.W., Dolean, V. and Heinlein, A., 2026. Are Deep Learning Based Hybrid PDE Solvers Reliable? Why Training Paradigms and Update Strategies Matter. arXiv preprint arXiv:2602.06842.

    \bibitem{Lynch1964}
    Lynch, R.E., Rice, J.R. and Thomas, D.H., 1964. Direct solution of partial difference equations by tensor product methods. Numerische Mathematik, 6(1), pp.185-199.

    \bibitem{de2011geopdes}
    De Falco, C., Reali, A. and Vázquez, R., 2011. GeoPDEs: a research tool for isogeometric analysis of PDEs. Advances in Engineering Software, 42(12), pp.1020-1034.

    \bibitem{de1978practical}
    De Boor, C. and De Boor, C., 1978. A practical guide to splines (Vol. 27, p. 325). New York: springer.

    \bibitem{fanaskov2023spectral}
    Fanaskov, V.S. and Oseledets, I.V., 2023, December. Spectral neural operators. In Doklady Mathematics (Vol. 108, No. Suppl 2, pp. S226-S232). Moscow: Pleiades Publishing.

    \bibitem{montardini2025isogeometric}
    Montardini, M., Takacs, S. and Tani, M., 2025. The Isogeometric Fast Fourier-based Diagonalization method. arXiv preprint arXiv:2512.20269.

    \bibitem{cottrell2009isogeometric}
    Cottrell, J.A., Hughes, T.J. and Bazilevs, Y., 2009. Isogeometric analysis: toward integration of CAD and FEA. John Wiley $\&$ Sons.

    \bibitem{adler2010geometry}
    Adler, R.J., 2010. The geometry of random fields. Society for Industrial and Applied Mathematics.

    \bibitem{saad1993flexible}
    Saad, Y., 1993. A flexible inner-outer preconditioned GMRES algorithm. SIAM Journal on Scientific Computing, 14(2), pp.461-469.

    \bibitem{ghiotto2025hypernos}
    Ghiotto, M., 2025. HyperNOs: automated and parallel library for neural operators research. Bollettino dell'Unione Matematica Italiana, pp.1-35.

    \bibitem{srivastava2014dropout}
    Srivastava, N., Hinton, G., Krizhevsky, A., Sutskever, I. and Salakhutdinov, R., 2014. Dropout: a simple way to prevent neural networks from overfitting. The journal of machine learning research, 15(1), pp.1929-1958.

    \bibitem{math10214014}
    Cui, C., Jiang, K., Liu, Y. and Shu, S., 2022. Fourier neural solver for large sparse linear algebraic systems. Mathematics, 10(21), p.4014.

    \bibitem{vaswani2017attention}
    Vaswani, A., Shazeer, N., Parmar, N., Uszkoreit, J., Jones, L., Gomez, A.N., Kaiser, Ł. and Polosukhin, I., 2017. Attention is all you need. Advances in neural information processing systems, 30.

    \bibitem{he2016identity}
    He, K., Zhang, X., Ren, S. and Sun, J., 2016, September. Identity mappings in deep residual networks. In European conference on computer vision (pp. 630-645). Cham: Springer International Publishing.

    \bibitem{he2016deep}
    He, K., Zhang, X., Ren, S. and Sun, J., 2016. Deep residual learning for image recognition. In Proceedings of the IEEE conference on computer vision and pattern recognition (pp. 770-778).

    \bibitem{kahana2023geometry}
    Kahana, A., Zhang, E., Goswami, S., Karniadakis, G., Ranade, R. and Pathak, J., 2023. On the geometry transferability of the hybrid iterative numerical solver for differential equations. Computational Mechanics, 72(3), pp.471-484.

    \bibitem{kopanivcakova2025deeponet}
    Kopaničáková, A. and Karniadakis, G.E., 2025. Deeponet based preconditioning strategies for solving parametric linear systems of equations. SIAM Journal on Scientific Computing, 47(1), pp.C151-C181.

    \bibitem{brenner2008mathematical}
    Brenner, S.C. and Scott, L.R., 2008. The mathematical theory of finite element methods. New York, NY: Springer New York.

    \bibitem{sampath2010parallel}
    Sampath, R.S. and Biros, G., 2010. A parallel geometric multigrid method for finite elements on octree meshes. SIAM Journal on Scientific Computing, 32(3), pp.1361-1392.

    \bibitem{lee2025hybrid}
    Lee, Y., Florencio, F.L., Pathak, J. and Karniadakis, G.E., 2025. Hybrid Iterative Solvers with Geometry-Aware Neural Preconditioners for Parametric PDEs. arXiv preprint arXiv:2512.14596.

    \bibitem{chen1995universal}
    Chen, T. and Chen, H., 1995. Universal approximation to nonlinear operators by neural networks with arbitrary activation functions and its application to dynamical systems. IEEE transactions on neural networks, 6(4), pp.911-920.

    \bibitem{hughes2005isogeometric}
    Hughes, T.J., Cottrell, J.A. and Bazilevs, Y., 2005. Isogeometric analysis: CAD, finite elements, NURBS, exact geometry and mesh refinement. Computer methods in applied mechanics and engineering, 194(39-41), pp.4135-4195.

    \bibitem{hendrycks2016gaussian}
    Hendrycks, D. and Gimpel, K., 2016. Gaussian error linear units (gelus). arXiv preprint arXiv:1606.08415.

    \bibitem{watanabe2023tree}
    Watanabe, S., 2023. Tree-structured parzen estimator: Understanding its algorithm components and their roles for better empirical performance. arXiv preprint arXiv:2304.11127.

    \bibitem{bergstra2015hyperopt}
    Bergstra, J., Komer, B., Eliasmith, C., Yamins, D. and Cox, D.D., 2015. Hyperopt: a python library for model selection and hyperparameter optimization. Computational Science $\&$ Discovery, 8(1), p.014008.

    \bibitem{li2020massivelyparallelhyperparametertuning}
    Li, L., Jamieson, K., Rostamizadeh, A., Gonina, E., Ben-Tzur, J., Hardt, M., Recht, B. and Talwalkar, A., 2020. A system for massively parallel hyperparameter tuning. Proceedings of machine learning and systems, 2, pp.230-246.

    \bibitem{loshchilov2019decoupledweightdecayregularization}
    Loshchilov, I. and Hutter, F., 2017. Decoupled weight decay regularization. arXiv preprint arXiv:1711.05101.

    \bibitem{sangalli2016isogeometric}
    Sangalli, G. and Tani, M., 2016. Isogeometric preconditioners based on fast solvers for the Sylvester equation. SIAM Journal on Scientific Computing, 38(6), pp.A3644-A3671.

    \bibitem{gahalaut2019condition}
    Gahalaut, K.P., Tomar, S.K. and Douglas, C., 2014. Condition number estimates for matrices arising in NURBS based isogeometric discretizations of elliptic partial differential equations. arXiv preprint arXiv:1406.6808.

    \bibitem{trottenberg2000multigrid}
    Trottenberg, U., Oosterlee, C.W. and Schuller, A., 2001. Multigrid methods. Academic press.

    \bibitem{toselli2004domain}
    Toselli, A. and Widlund, O., 2004. Domain decomposition methods-algorithms and theory (Vol. 34). Springer Science $\&$ Business Media.

    \bibitem{lu2021learning}
    Lu, L., Jin, P., Pang, G., Zhang, Z. and Karniadakis, G.E., 2021. Learning nonlinear operators via DeepONet based on the universal approximation theorem of operators. Nature machine intelligence, 3(3), pp.218-229.

    \bibitem{li2021fourier}
    Li, Z., Kovachki, N., Azizzadenesheli, K., Liu, B., Bhattacharya, K., Stuart, A. and Anandkumar, A., 2020. Fourier neural operator for parametric partial differential equations. arXiv preprint arXiv:2010.08895.

    \bibitem{rahaman2019spectral}
    Rahaman, N., Baratin, A., Arpit, D., Draxler, F., Lin, M., Hamprecht, F., Bengio, Y. and Courville, A., 2019, May. On the spectral bias of neural networks. In International conference on machine learning (pp. 5301-5310). PMLR.

    \bibitem{tielen2020pmultigrid}
    Tielen, R.P.W.M., Möller, M., Göddeke, D. and Vuik, C., 2020. p-multigrid methods and their comparison to h-multigrid methods within Isogeometric Analysis. Computer Methods in Applied Mechanics and Engineering, 372, p.113347.

    \bibitem{moller2026iganet}
    Möller, M., Obermair, G., Singer, I., Gollmann, C., Reali, A. and Elgeti, S., 2026. IGANets: Isogeometric analysis networks and their applications to linear structural analysis problems. Engineering with Computers, 42(3), p.102.

    \bibitem{karniadakis2025nonlinearprec}
    Lee, Y., Liu, S., Darbon, J. and Karniadakis, G.E., 2025. A Neural-Operator Preconditioned Newton Method for Accelerated Nonlinear Solvers. arXiv preprint arXiv:2511.08811.

    \bibitem{karniadakis2025nspod}
    Levrero-Florencio, F., Lee, Y., Pathak, J. and Karniadakis, G.E., 2026. NSPOD: acceleratingthe convergence ofKrylov-based iterative linearsolvers via approximated PODs. arXiv preprint arXiv:2605.07828.

\end{thebibliography}
% \printbibliography

\end{document}
